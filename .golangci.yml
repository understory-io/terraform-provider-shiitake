version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - gocritic
    - unconvert
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - gocritic
